package serveur;
/*
import identification.Identification;
import inventaire.InventaireJoueur;
import message.Message;
import operation.OpAchatNouvelleAction;
import operation.Operation;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import reseau.EchangesAvecLeClient;

import java.util.HashMap;

import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.times;
import static org.junit.jupiter.api.Assertions.assertEquals;

@ExtendWith(MockitoExtension.class)
 */
class ServeurTest
{
/*
    Serveur serveurTesté;
    private HashMap<Identification, InventaireJoueur> HMinventaires = new HashMap<Identification, InventaireJoueur>();
    Identification joueurTest;
    InventaireJoueur inventaire = new InventaireJoueur();
    Operation operationATrouver = new OpAchatNouvelleAction();

    @Mock
    EchangesAvecLeClient connexion;

    @BeforeEach
    void setUp() {
        serveurTesté = new Serveur("127.0.0.1",10101);
        serveurTesté.setConnexion(connexion);
        joueurTest = new Identification("joueur pour le test");
        inventaire = new InventaireJoueur();
        HMinventaires.put(joueurTest,inventaire);
        serveurTesté.nouveauJoueur(joueurTest);
    }

    @Test
    void faireUnTour() {
        serveurTesté.transfereAction(operationATrouver);
    }

    @Test
    void nouveauJoueur() {
        assertEquals(1, serveurTesté.getMoteur().getJoeurs().length);
        serveurTesté.nouveauJoueur(joueurTest);
        // refus de la connexion
        assertEquals(1, serveurTesté.getMoteur().getJoeurs().length);
    }*/
}